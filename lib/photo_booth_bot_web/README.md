# PhotoBoothBotWeb

Deletable demo admin UI.

This folder is the example app-specific admin area. For a real bot, replace it with pages like campaigns, orders, prizes, reports, moderation, or custom dashboards.

Generic bot admin stays under `lib/bot_machine_web/controllers/admin/bot_*`.
